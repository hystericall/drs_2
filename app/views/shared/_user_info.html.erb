<%= link_to gravatar_for(current_user, size: Settings.index_ava_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view"), current_user %></span>
<span><%= I18n.t(:request, count: current_user.requests.size) %></span>
